* Inputs
  * Clearinterval/setinterval with each down move
  * Pause inputs when clearing lines
* Accelerate tempo (levels)
* Design (borders + nextpiece + current level)
* Highlight last piece during merging?
